# ===========================================
# Nara Market FastMCP Server - 환경변수 설정
# ===========================================
# 이 파일을 .env로 복사한 후 실제 값을 입력하세요:
#   cp .env.example .env

# -------------------------------------------
# 필수 설정
# -------------------------------------------

# 나라장터(G2B) API 서비스 키
# https://www.data.go.kr/ 에서 발급받은 인코딩된 서비스 키
NARAMARKET_SERVICE_KEY=your_api_key_here

# -------------------------------------------
# FastMCP 서버 설정 (선택)
# -------------------------------------------

# MCP Transport 모드: stdio, sse, http
# FASTMCP_TRANSPORT=stdio

# HTTP/SSE 모드 호스트 (FASTMCP_TRANSPORT=http 또는 sse일 때)
# FASTMCP_HOST=127.0.0.1

# HTTP/SSE 모드 포트 (FASTMCP_TRANSPORT=http 또는 sse일 때)
# FASTMCP_PORT=8081

# 클라우드 배포 시 포트 (Smithery.ai 등, FASTMCP_PORT보다 우선)
# PORT=8081

# -------------------------------------------
# API 환경 설정 (선택)
# -------------------------------------------

# API 환경: production, development
# API_ENVIRONMENT=production

# 로깅 레벨: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO

# -------------------------------------------
# 인증 설정 (선택)
# -------------------------------------------

# JWT 시크릿 키 (미설정 시 기본값 사용, 프로덕션에서는 반드시 변경)
# JWT_SECRET_KEY=your_jwt_secret_key_here

# Access Token 만료 시간 (분)
# ACCESS_TOKEN_EXPIRE_MINUTES=60

# Refresh Token 만료 시간 (일)
# REFRESH_TOKEN_EXPIRE_DAYS=30
